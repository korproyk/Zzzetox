services:
  - type: web
    name: sleep-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn sleep_app:app --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT
    envVars:
      - key: SLEEP_APP_DATA_DIR
        value: /opt/render/project/src/data
